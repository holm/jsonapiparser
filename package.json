{
  "name": "jsonapiparser",
  "version": "0.1.1",
  "description": "Parse JSONAPI data",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^10.2.1",
    "uglifyjs": "^2.4.10"
  },
  "author": "Tobias Baunbæk <freeall@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/freeall/jsonapiparser.git"
  },
  "scripts": {
    "create-build": "browserify index.js -s jsonapiparser > builds/jsonapiparser.${npm_package_version}.js && browserify index.js -s jsonapiparser | uglifyjs -c > builds/jsonapiparser.${npm_package_version}.min.js",
    "create-release": "browserify index.js -s jsonapiparser > builds/jsonapiparser.js && browserify index.js -s jsonapiparser | uglifyjs -c > builds/jsonapiparser.min.js"
  }
}
